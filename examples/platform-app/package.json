{
    "name": "platform-app",
    "version": "8.0.0-alpha.6",
    "description": "",
    "license": "BSD-3-Clause",
    "private": true,
    "scripts": {
        "build": "d2-app-scripts build",
        "start": "d2-app-scripts start",
        "test": "d2-app-scripts test",
        "lint": "d2-style check",
        "lint:staged": "yarn lint --staged",
        "format": "d2-style apply",
        "format:staged": "yarn format --staged",
        "cy:start": "BROWSER=none yarn start",
        "cy:open": "concurrently 'yarn:cy:start' 'd2-utils-cypress open' --kill-others",
        "cy:run": "concurrently 'yarn:cy:start' 'd2-utils-cypress run' --kill-others",
        "cy:capture": "concurrently 'yarn:cy:start' 'd2-utils-cypress run --capture --serverMinorVersion 37' --kill-others",
        "cy:stub": "concurrently 'yarn:cy:start' 'd2-utils-cypress run --stub --serverMinorVersion 37' --kill-others",
        "cy:capture-and-stub": "yarn cy:capture && yarn cy:stub"
    },
    "dependencies": {
        "@dhis2/app-runtime": "^2.8.0",
        "prop-types": "^15.7.2"
    }
}
